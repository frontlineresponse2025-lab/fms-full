# URL of your deployed API server (Railway URL or custom domain)
# Example: https://fms-api.frontlineresponse.com
# Leave blank if running the frontend and backend on the same host
VITE_API_URL=https://your-api.railway.app
